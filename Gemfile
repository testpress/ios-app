source "https://rubygems.org"

ruby ">= 3.0.0"

gem "fastlane", ">= 2.210.0"
gem "mini_magick", ">= 4.11.0"
gem "xcodeproj", ">= 1.22.0"

# Required transitively by `representable` (used by google-apis-* gems that
# fastlane loads at startup via the create_app_on_managed_play_store action).
# Not declared by fastlane itself, so must be explicit.
gem "multi_json", ">= 1.15.0"
